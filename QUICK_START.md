# 🚀 دليل البدء السريع - خطوة بخطوة

## 📌 الخطوات المطلوبة للبدء من الصفر:

---

## 1️⃣ حذف البيانات القديمة (إذا كانت موجودة)

### الطريقة الأولى: من لوحة التحكم
1. افتح `clear-data.html` في المتصفح
2. اضغط على "مسح جميع البيانات"
3. أكد الحذف مرتين
4. انتظر حتى تظهر رسالة "تم المسح بنجاح"

### الطريقة الثانية: من Firebase Console (اختياري)
إذا كنت تريد حذف حسابات المدراء أيضاً:
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك: `test-97ecc`
3. Authentication → Users
4. احذف جميع المستخدمين

---

## 2️⃣ إنشاء حساب المدير

1. افتح `setup.html` في المتصفح
2. املأ البيانات:
   ```
   البريد الإلكتروني: admin@example.com
   كلمة المرور: ******* (6 أحرف على الأقل)
   تأكيد كلمة المرور: *******
   ```
3. اضغط "إنشاء الحساب"
4. سيتم تحويلك للوحة التحكم تلقائياً ✅

---

## 3️⃣ إضافة أول منتج

1. من لوحة التحكم، اذهب إلى **"المنتجات"**
2. اضغط **"إضافة منتج جديد"**
3. املأ بيانات المنتج:
   ```
   اسم المنتج: شيبسي
   السعر: 50
   صورة المنتج: [اختر صورة من جهازك]
   الوصف: شيبسي لذيذ (اختياري)
   ```
4. اضغط **"حفظ المنتج"**
5. ستظهر رسالة "تم إضافة المنتج بنجاح!" ✅

---

## 4️⃣ التحقق من عمل النظام

### تحقق من المنتجات:
- اذهب إلى صفحة **"المنتجات"**
- يجب أن ترى المنتج الذي أضفته
- جرّب حذف منتج

### تحقق من الداش بورد:
- اذهب إلى **"الرئيسية"**
- تحقق من الإحصائيات:
  - ✅ إجمالي المنتجات: 1 (أو حسب ما أضفت)
  - ✅ الطلبات: 0
  - ✅ المبيعات: 0 EGP

---

## 5️⃣ إعداد قواعد الأمان (مهم!)

### Firestore Security Rules:
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. Firestore Database → Rules
3. انسخ القواعد من ملف `FIREBASE_SECURITY_RULES.md`
4. الصق في المحرر
5. اضغط **"Publish"**

### Storage Security Rules:
1. Storage → Rules
2. انسخ قواعد Storage من نفس الملف
3. الصق في المحرر
4. اضغط **"Publish"**

---

## 6️⃣ ربط الداش بورد بالموقع (اختياري)

إذا كان لديك موقع عام:

### في موقعك العام (Website):
استخدم نفس `firebase-config.js` للوصول لنفس البيانات:

```javascript
// في موقعك
import { db, collection, getDocs } from './firebase-config.js';

// جلب المنتجات
async function loadProducts() {
    const snapshot = await getDocs(collection(db, "products"));
    snapshot.forEach(doc => {
        const product = doc.data();
        console.log(product.name, product.price);
    });
}
```

---

## ✅ قائمة التحقق النهائية

- [ ] تم حذف البيانات القديمة
- [ ] تم إنشاء حساب المدير الأول
- [ ] تم إضافة منتج تجريبي
- [ ] تم تطبيق قواعد Firebase Security Rules
- [ ] تم اختبار إضافة/حذف المنتجات
- [ ] تم ربط الداش بورد بالموقع (إذا كان موجود)

---

## 🎯 ملاحظات مهمة:

### 🔐 الأمان:
- **لا تشارك بيانات Firebase Config مع أحد**
- غيّر Email/Password للمدير بعد الإعداد
- فعّل Two-Factor Authentication في Firebase Console

### 📂 رفع الصور:
- الصور تُرفع تلقائياً إلى Firebase Storage
- الحد الأقصى: 5 ميجا
- الصيغ المدعومة: JPG, PNG, WebP, GIF

### 🌐 النشر:
يمكنك نشر الداش بورد على:
- Firebase Hosting
- Netlify
- Vercel
- أي استضافة ثابتة (Static Hosting)

---

## 🆘 مشاكل شائعة:

### ❌ "Access Denied" عند إضافة منتج
**الحل:** تأكد من تطبيق Firebase Security Rules

### ❌ الصور لا تظهر
**الحل:** تأكد من تفعيل Firebase Storage وتطبيق القواعد

### ❌ لا يمكن تسجيل الدخول
**الحل:** تأكد من تفعيل Email/Password في Firebase Authentication

---

## 📞 تواصل:
إذا واجهت أي مشاكل، افحص:
1. Console في المتصفح (F12)
2. Firebase Console → Logs
3. ملف `README.md` للمزيد من التفاصيل

---

**🎉 مبروك! الداش بورد جاهز للعمل!**

الآن يمكن لصاحب الموقع:
- ✅ إضافة منتجات جديدة
- ✅ رفع الصور
- ✅ إدارة الطلبات
- ✅ متابعة المبيعات
