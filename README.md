# 🎯 لوحة التحكم - دليل المستخدم

## 📋 نظرة عامة
لوحة تحكم متكاملة لإدارة المتجر الإلكتروني مع دعم Firebase للمصادقة، قاعدة البيانات، والتخزين السحابي.

---

## 🚀 البدء من الصفر

### 1. مسح البيانات القديمة
إذا كنت تريد البدء من جديد:
1. افتح الرابط: `clear-data.html`
2. اضغط على "مسح جميع البيانات"
3. سيتم حذف جميع المنتجات والطلبات والبيانات

**ملاحظة:** حسابات المدراء في Firebase Authentication لن يتم حذفها تلقائياً. يمكنك حذفها يدوياً من:
- Firebase Console → Authentication → Users

### 2. إنشاء حساب المدير الأول
1. افتح الرابط: `setup.html`
2. أدخل البريد الإلكتروني وكلمة المرور
3. اضغط "إنشاء الحساب"
4. سيتم تحويلك تلقائياً للوحة التحكم

---

## 🔐 تسجيل الدخول

### للمدير
1. افتح الرابط: `index.html`
2. أدخل البريد الإلكتروني وكلمة المرور
3. اضغط "تسجيل الدخول"

---

## 📦 إدارة المنتجات

### إضافة منتج جديد
1. اذهب إلى صفحة "المنتجات"
2. اضغط على "إضافة منتج جديد"
3. املأ البيانات:
   - اسم المنتج
   - السعر
   - **صورة المنتج**: اختر صورة من جهازك (سيتم رفعها تلقائياً إلى Firebase Storage)
   - الوصف (اختياري)
4. اضغط "حفظ المنتج"

### حذف منتج
1. اذهب إلى صفحة "المنتجات"
2. اضغط على زر "حذف" تحت أي منتج
3. تأكيد الحذف

**ملاحظة:** عند حذف منتج، الصورة ستبقى في Firebase Storage. يمكنك حذفها يدوياً من Firebase Console إذا أردت.

---

## 🛒 إدارة الطلبات

### عرض الطلبات
- اذهب إلى صفحة "الطلبات"
- ستظهر جميع الطلبات مع تفاصيلها

### تحديث حالة الطلب
استخدم الأزرار:
- ✅ زر الصح: لتحديد الطلب كـ "مكتمل"
- ❌ زر X: لإلغاء الطلب

---

## ⚙️ الإعدادات

### الإعدادات العامة
- اسم المتجر
- رسالة الترحيب

### منطقة خطرة ⚠️
- **مسح جميع البيانات**: يحذف جميع المنتجات والطلبات والبيانات (لا يمكن التراجع!)

---

## 🗂️ بنية الملفات

```
داش بورد/
├── index.html           # صفحة تسجيل الدخول
├── setup.html           # صفحة إنشاء الحساب الأول
├── clear-data.html      # صفحة مسح البيانات
├── dashboard.html       # الصفحة الرئيسية
├── products.html        # إدارة المنتجات
├── orders.html          # إدارة الطلبات
├── users.html           # إدارة المستخدمين
├── settings.html        # الإعدادات
├── style.css            # التنسيقات
└── js/
    ├── firebase-config.js   # إعدادات Firebase
    ├── auth.js              # تسجيل الدخول
    ├── setup.js             # إنشاء الحساب
    └── main.js              # الوظائف الرئيسية
```

---

## 🔧 إعدادات Firebase

### المتطلبات
- Firebase Project
- تفعيل Authentication (Email/Password)
- تفعيل Firestore Database
- تفعيل Storage

### Collections في Firestore
- `products`: المنتجات
- `orders`: الطلبات
- `users`: المستخدمين (اختياري)

---

## ✨ المميزات

### ✅ الحالية
- تسجيل دخول آمن
- إضافة/حذف المنتجات
- رفع الصور مباشرة من الجهاز
- معاينة الصور قبل الرفع
- إدارة الطلبات
- إحصائيات في الداش بورد
- واجهة عربية جميلة مع Dark Mode

### 🔜 قادماً
- تعديل المنتجات
- البحث والفلترة
- تصدير البيانات
- إضافة مدراء جدد
- إدارة الصلاحيات

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يمكن تسجيل الدخول
**الحل:**
- تأكد من أن البريد الإلكتروني وكلمة المرور صحيحة
- تأكد من تفعيل Email/Password في Firebase Authentication

### المشكلة: الصور لا يتم رفعها
**الحل:**
- تأكد من تفعيل Firebase Storage
- تأكد من أن الصورة بصيغة صحيحة (JPG, PNG, WebP)
- تحقق من حجم الصورة (لا تتجاوز 5 ميجا)

### المشكلة: البيانات لا تظهر
**الحل:**
- تحقق من Console في المتصفح (F12)
- تأكد من إعدادات Firebase Firestore Security Rules

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. افحص Console في المتصفح (اضغط F12)
2. تحقق من إعدادات Firebase
3. تأكد من أن جميع الملفات موجودة

---

## 🎨 التخصيص

### تغيير الألوان
عدّل ملف `style.css`:
```css
:root {
    --primary: #6366f1;  /* اللون الأساسي */
    --success: #10b981;  /* لون النجاح */
    --danger: #ef4444;   /* لون الخطر */
}
```

### تغيير اسم المتجر
عدّل في جميع ملفات HTML:
```html
<h4 class="fw-bold tracking-wide m-0">اسم متجرك</h4>
```

---

## 📄 الترخيص
هذا المشروع مجاني للاستخدام الشخصي والتجاري.

---

**تم التطوير بـ ❤️ بواسطة Gemini AI**
